# Protect config.json from direct access
<Files "config.json">
    Require all denied
</Files>

# Allow iframe embedding from any domain
# Remove X-Frame-Options to allow embedding (deprecated header)
Header always unset X-Frame-Options
# Allow embedding from any domain using CSP
Header always set Content-Security-Policy "frame-ancestors *"
